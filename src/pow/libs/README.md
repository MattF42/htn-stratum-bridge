Here we lazily provide pre built versions of libhoohash.a and libblake3.a 
These are used when we are doing CGO version of the solver

Each exists in it's own subdirectory

Currently on linux-aarch64 is supported by this

